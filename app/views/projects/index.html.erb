<div>
  <div class="projects-title">
    <h2>My web projects</h2>
  </div>

  <div class="projects-body">
    <p>
      Other projects are coming soon - do not hesitate to contact me to work together or if you have any suggestions.
    </p>
  </div>


  <div class="mt-5">
    <div class="carroussel d-flex row justify-content-center my-3">

      <div class="col-4">
        <% @projects.each do |project| %>
        <h3>Project: <%= project.title %></h3>
        <p><%= project.description %></p>
        <a href="<%= project.ghrepo %>" target="_blank"><i class="fab fa-github mx-1"></i>Check Github repo</a>
      </div>

      <div id="carouselExampleControls" class="carousel slide col-8" data-bs-ride="carousel" width="700">
        <div class="carousel-inner mt-2 d-grid justify-content-center">

          <% project.photos.each_with_index do |photo, index| %>
            <div class="carousel-item <%='active' if index == 0 %>">
              <%= image_tag url_for(photo), height: 500, width: 650, crop: :fill, class:'show-img', alt: "Project website screenshot" %>
            </div>
          <% end %>
        <% end %>
          <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
          <span class="carousel-control-prev-icon arrow-color" aria-hidden="true"></span>
          <span class="visually-hidden">Previous</span>
          </button>
          <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
          <span class="carousel-control-next-icon arrow-color" aria-hidden="true"></span>
          <span class="visually-hidden">Next</span>
          </button>
        </div>
      </div>

    </div>
  </div>

</div>
